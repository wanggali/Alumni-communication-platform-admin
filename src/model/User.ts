type User = {
    id: bigint,
    rid: bigint,
    collegeId: bigint,
    adminType: number,
    email: string,
    avatar: string,
    username: string,
    sign: string,
    status: number,
    joinTime: string
}

export default User;
